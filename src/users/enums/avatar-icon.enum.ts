// A curated set of themed icon avatars a member can pick instead of an
// initial-letter fallback — always available, independent of the
// allowProfileImageUpload setting (see SiteSettings). Values mirror the
// WS-Web Icon component's icon names for these entries.
export enum AvatarIcon {
  Ghost = 'ghost',
  Moon = 'moon',
  Skull = 'skull',
  Bat = 'bat',
  Spider = 'spider',
  Pumpkin = 'pumpkin',
  Candle = 'candle',
  Eye = 'eye',
}
